# AmbuSOS Backend

Express + MongoDB backend written in JavaScript.

## Scripts

- `npm run dev` starts the backend in watch mode.
- `npm start` starts the backend normally.
- `npm run lint` runs ESLint on `src-mern`.
